export default function StaffDashboardPage() {
  return (
    <main style={{ padding: '32px', maxWidth: '420px', margin: '0 auto' }}>
      <h1 style={{ fontSize: '22px' }}>Staff counter</h1>
      <p style={{ color: 'var(--ink-soft)', fontSize: '14px' }}>
        Scan and reports go here — next up.
      </p>
    </main>
  );
}
